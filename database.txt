-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.ai_recommender_outputs (
  request_id uuid NOT NULL,
  output_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_recommender_outputs_pkey PRIMARY KEY (request_id),
  CONSTRAINT ai_recommender_outputs_request_id_fkey FOREIGN KEY (request_id) REFERENCES public.ai_recommender_requests(id)
);
CREATE TABLE public.ai_recommender_product_scores (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  request_id uuid NOT NULL,
  product_id uuid,
  score numeric,
  reason text,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_recommender_product_scores_pkey PRIMARY KEY (id),
  CONSTRAINT ai_recommender_product_scores_request_id_fkey FOREIGN KEY (request_id) REFERENCES public.ai_recommender_requests(id),
  CONSTRAINT ai_recommender_product_scores_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.ai_recommender_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_by_user_id uuid,
  lead_id uuid,
  provider text NOT NULL DEFAULT 'gemini'::text,
  model text,
  status text NOT NULL DEFAULT 'pending'::text,
  input_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  CONSTRAINT ai_recommender_requests_pkey PRIMARY KEY (id),
  CONSTRAINT ai_recommender_requests_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.profiles(user_id),
  CONSTRAINT ai_recommender_requests_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id)
);
CREATE TABLE public.audit_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  actor_user_id uuid,
  action_type text NOT NULL,
  entity_type text,
  entity_id uuid,
  description text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT audit_log_pkey PRIMARY KEY (id),
  CONSTRAINT audit_log_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES public.profiles(user_id)
);
CREATE TABLE public.cms_revisions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  section_id uuid NOT NULL,
  revision integer NOT NULL,
  content jsonb NOT NULL,
  changed_by uuid,
  change_note text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT cms_revisions_pkey PRIMARY KEY (id),
  CONSTRAINT cms_revisions_section_id_fkey FOREIGN KEY (section_id) REFERENCES public.cms_sections(id),
  CONSTRAINT cms_revisions_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.cms_sections (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  section_key text NOT NULL,
  scope_type USER-DEFINED NOT NULL DEFAULT 'global'::scope_type_enum,
  scope_id uuid,
  content jsonb NOT NULL DEFAULT '{}'::jsonb,
  revision bigint NOT NULL DEFAULT 1,
  updated_by_user_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_published boolean NOT NULL DEFAULT true,
  CONSTRAINT cms_sections_pkey PRIMARY KEY (id),
  CONSTRAINT cms_sections_updated_by_user_id_fkey FOREIGN KEY (updated_by_user_id) REFERENCES public.profiles(user_id)
);
CREATE TABLE public.comparison_session_products (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  session_id uuid NOT NULL,
  product_id uuid NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  added_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT comparison_session_products_pkey PRIMARY KEY (id),
  CONSTRAINT comparison_session_products_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.comparison_sessions(id),
  CONSTRAINT comparison_session_products_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.comparison_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_by_user_id uuid,
  session_token text UNIQUE,
  status text NOT NULL DEFAULT 'active'::text,
  settings jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT comparison_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT comparison_sessions_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.profiles(user_id)
);
CREATE TABLE public.email_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_key text NOT NULL UNIQUE,
  name text,
  trigger_event text,
  audience text,
  template_subject text,
  template_body text,
  variables jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT email_templates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.escorts_template_chapters (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  template_id uuid NOT NULL,
  chapter_key text NOT NULL,
  chapter_number text NOT NULL,
  title text NOT NULL,
  default_tagline text,
  interaction_type USER-DEFINED NOT NULL DEFAULT 'spec_grid'::chapter_interaction,
  display_order integer NOT NULL DEFAULT 0,
  is_required boolean NOT NULL DEFAULT true,
  config jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT escorts_template_chapters_pkey PRIMARY KEY (id),
  CONSTRAINT escorts_template_chapters_template_id_fkey FOREIGN KEY (template_id) REFERENCES public.escorts_templates(id)
);
CREATE TABLE public.escorts_templates (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  slug text NOT NULL UNIQUE,
  name text NOT NULL,
  brand_id uuid,
  category_id uuid,
  layout USER-DEFINED NOT NULL DEFAULT 'scroll_story'::showcase_layout,
  chapter_count integer NOT NULL DEFAULT 10,
  description text,
  is_default boolean NOT NULL DEFAULT false,
  config jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT escorts_templates_pkey PRIMARY KEY (id),
  CONSTRAINT escorts_templates_brand_id_fkey FOREIGN KEY (brand_id) REFERENCES public.product_brands(id),
  CONSTRAINT escorts_templates_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.product_categories(id)
);
CREATE TABLE public.integrations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  integration_key text NOT NULL UNIQUE,
  status text NOT NULL DEFAULT 'not_connected'::text,
  config jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT integrations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.lead_insights (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  insight_type text NOT NULL,
  title text,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  source text NOT NULL DEFAULT 'n8n'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT lead_insights_pkey PRIMARY KEY (id),
  CONSTRAINT lead_insights_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id)
);
CREATE TABLE public.lead_selected_products (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  product_id uuid NOT NULL,
  selection_source text NOT NULL DEFAULT 'ui'::text,
  selected_at timestamp with time zone NOT NULL DEFAULT now(),
  context jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT lead_selected_products_pkey PRIMARY KEY (id),
  CONSTRAINT lead_selected_products_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_selected_products_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.lead_timeline_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  action text NOT NULL,
  by_user_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT lead_timeline_events_pkey PRIMARY KEY (id),
  CONSTRAINT lead_timeline_events_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_timeline_events_by_user_id_fkey FOREIGN KEY (by_user_id) REFERENCES public.profiles(user_id)
);
CREATE TABLE public.lead_whatsapp_threads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  from_phone text NOT NULL,
  to_phone text,
  lead_id uuid,
  thread_key text UNIQUE,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT lead_whatsapp_threads_pkey PRIMARY KEY (id),
  CONSTRAINT lead_whatsapp_threads_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id)
);
CREATE TABLE public.leads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_name text NOT NULL,
  company text NOT NULL,
  designation text,
  phone text,
  email text,
  city text,
  state text,
  kva_required text,
  application text,
  stage USER-DEFINED NOT NULL DEFAULT 'new'::lead_stage_enum,
  source USER-DEFINED NOT NULL DEFAULT 'website_form'::lead_source_enum,
  assigned_to_user_id uuid,
  assigned_to_name text,
  score integer NOT NULL DEFAULT 0,
  created_by_user_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  last_activity_at timestamp with time zone,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT leads_pkey PRIMARY KEY (id),
  CONSTRAINT leads_assigned_to_user_id_fkey FOREIGN KEY (assigned_to_user_id) REFERENCES public.profiles(user_id),
  CONSTRAINT leads_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.profiles(user_id)
);
CREATE TABLE public.notification_settings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  new_lead boolean NOT NULL DEFAULT true,
  lead_assigned boolean NOT NULL DEFAULT true,
  quote_sent boolean NOT NULL DEFAULT true,
  followup_due boolean NOT NULL DEFAULT true,
  amc_renewal boolean NOT NULL DEFAULT true,
  service_ticket boolean NOT NULL DEFAULT false,
  weekly_report boolean NOT NULL DEFAULT true,
  monthly_report boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT notification_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.presentation_session_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  session_id uuid NOT NULL,
  event_type text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT presentation_session_events_pkey PRIMARY KEY (id),
  CONSTRAINT presentation_session_events_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.presentation_sessions(id)
);
CREATE TABLE public.presentation_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_by_user_id uuid,
  share_token text UNIQUE,
  product_id uuid,
  cms_section_key text NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'active'::presentation_status_enum,
  current_chapter_index integer NOT NULL DEFAULT 0,
  current_hotspot_id text,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  last_activity_at timestamp with time zone NOT NULL DEFAULT now(),
  ended_at timestamp with time zone,
  CONSTRAINT presentation_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT presentation_sessions_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.profiles(user_id),
  CONSTRAINT presentation_sessions_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_attribute_definitions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  category_id uuid,
  brand_id uuid,
  attribute_key text NOT NULL,
  label text NOT NULL,
  unit text,
  data_type text NOT NULL DEFAULT 'text'::text,
  enum_values ARRAY,
  is_required boolean NOT NULL DEFAULT false,
  is_filterable boolean NOT NULL DEFAULT false,
  is_comparable boolean NOT NULL DEFAULT true,
  group_name text DEFAULT 'General'::text,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_attribute_definitions_pkey PRIMARY KEY (id),
  CONSTRAINT product_attribute_definitions_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.product_categories(id),
  CONSTRAINT product_attribute_definitions_brand_id_fkey FOREIGN KEY (brand_id) REFERENCES public.product_brands(id)
);
CREATE TABLE public.product_attribute_values (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  attribute_def_id uuid NOT NULL,
  value_text text,
  value_numeric numeric,
  value_boolean boolean,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_attribute_values_pkey PRIMARY KEY (id),
  CONSTRAINT product_attribute_values_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT product_attribute_values_attribute_def_id_fkey FOREIGN KEY (attribute_def_id) REFERENCES public.product_attribute_definitions(id)
);
CREATE TABLE public.product_brands (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  slug text NOT NULL UNIQUE,
  name text NOT NULL,
  logo_url text,
  description text,
  website text,
  is_oem boolean NOT NULL DEFAULT false,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_brands_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_categories (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_id uuid,
  slug text NOT NULL UNIQUE,
  name text NOT NULL,
  description text,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  icon text,
  image_url text,
  seo_title text,
  meta_desc text,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT product_categories_pkey PRIMARY KEY (id),
  CONSTRAINT product_categories_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.product_categories(id)
);
CREATE TABLE public.product_chapter_data (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  chapter_key text NOT NULL,
  interaction_type USER-DEFINED NOT NULL DEFAULT 'spec_grid'::chapter_interaction,
  specs jsonb,
  features jsonb,
  badges jsonb,
  description text,
  about_specs jsonb,
  lube_specs jsonb,
  cooling_specs jsonb,
  perf_specs jsonb,
  reactance_data jsonb,
  acoustic_dims jsonb,
  open_dims jsonb,
  env_specs jsonb,
  engine_params jsonb,
  electrical_params jsonb,
  electrical_specs jsonb,
  engine_protections jsonb,
  electrical_protections jsonb,
  approvals jsonb,
  standard_items jsonb,
  optional_items jsonb,
  optional_groups jsonb,
  fuel_curve_data jsonb,
  efficiency_data jsonb,
  config jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_chapter_data_pkey PRIMARY KEY (id),
  CONSTRAINT product_chapter_data_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_documents (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid,
  doc_type text NOT NULL DEFAULT 'datasheet'::text,
  title text NOT NULL,
  storage_path text NOT NULL,
  public_url text,
  file_size_kb integer,
  mime_type text DEFAULT 'application/pdf'::text,
  page_count integer,
  uploaded_by uuid,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_documents_pkey PRIMARY KEY (id),
  CONSTRAINT product_documents_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT product_documents_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES auth.users(id)
);
CREATE TABLE public.product_edit_history (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  changed_by uuid,
  change_type text NOT NULL DEFAULT 'update'::text,
  diff jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_edit_history_pkey PRIMARY KEY (id),
  CONSTRAINT product_edit_history_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT product_edit_history_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.product_features (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  group_name text NOT NULL DEFAULT 'General'::text,
  feature_text text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_features_pkey PRIMARY KEY (id),
  CONSTRAINT product_features_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_hotspot_specs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  hotspot_id uuid NOT NULL,
  spec_label text NOT NULL,
  spec_value text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_hotspot_specs_pkey PRIMARY KEY (id),
  CONSTRAINT product_hotspot_specs_hotspot_id_fkey FOREIGN KEY (hotspot_id) REFERENCES public.product_hotspots(id)
);
CREATE TABLE public.product_hotspots (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  hotspot_key text NOT NULL,
  x numeric NOT NULL DEFAULT 50,
  y numeric NOT NULL DEFAULT 50,
  title text NOT NULL,
  description text,
  sub_image_url text,
  zoom numeric DEFAULT 1.0,
  offset_x numeric DEFAULT 0,
  offset_y numeric DEFAULT 0,
  display_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_hotspots_pkey PRIMARY KEY (id),
  CONSTRAINT product_hotspots_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_import_jobs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  document_id uuid,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::import_job_status,
  source_type text NOT NULL DEFAULT 'pdf'::text,
  source_url text,
  ai_provider text DEFAULT 'gemini'::text,
  ai_model text,
  raw_extraction jsonb,
  mapped_product jsonb,
  confidence_score numeric,
  missing_fields ARRAY,
  extraction_notes text,
  reviewed_by uuid,
  reviewed_at timestamp with time zone,
  target_product_id uuid,
  error_message text,
  processing_time_ms integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_import_jobs_pkey PRIMARY KEY (id),
  CONSTRAINT product_import_jobs_document_id_fkey FOREIGN KEY (document_id) REFERENCES public.product_documents(id),
  CONSTRAINT product_import_jobs_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES auth.users(id),
  CONSTRAINT product_import_jobs_target_product_id_fkey FOREIGN KEY (target_product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_media (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  product_id uuid NOT NULL,
  kind text NOT NULL,
  storage_path text,
  public_url text,
  alt_text text,
  mime_type text,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  caption text,
  file_size_kb integer,
  width_px integer,
  height_px integer,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT product_media_pkey PRIMARY KEY (id),
  CONSTRAINT product_media_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_presentation_config (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL UNIQUE,
  main_image_1 text,
  main_image_2 text,
  video_url text,
  video_thumb_url text,
  chapter_count integer NOT NULL DEFAULT 10,
  use_two_image boolean NOT NULL DEFAULT true,
  config jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_presentation_config_pkey PRIMARY KEY (id),
  CONSTRAINT product_presentation_config_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_showcase_section_specs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  section_id uuid NOT NULL,
  spec_label text NOT NULL,
  spec_value text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_showcase_section_specs_pkey PRIMARY KEY (id),
  CONSTRAINT product_showcase_section_specs_section_id_fkey FOREIGN KEY (section_id) REFERENCES public.product_showcase_sections(id)
);
CREATE TABLE public.product_showcase_sections (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  chapter_key text NOT NULL,
  chapter_number text NOT NULL,
  title text NOT NULL,
  tagline text,
  image_url text,
  video_url text,
  alt_text text,
  display_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  highlight jsonb,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_showcase_sections_pkey PRIMARY KEY (id),
  CONSTRAINT product_showcase_sections_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_specs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  product_id uuid NOT NULL,
  spec_label text NOT NULL,
  spec_value text NOT NULL,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  group_name text DEFAULT 'General'::text,
  unit text,
  is_highlight boolean NOT NULL DEFAULT false,
  CONSTRAINT product_specs_pkey PRIMARY KEY (id),
  CONSTRAINT product_specs_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.products (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  category_id uuid,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::product_status_enum,
  type USER-DEFINED NOT NULL,
  name text NOT NULL,
  model text NOT NULL,
  slug text NOT NULL UNIQUE,
  kva numeric NOT NULL CHECK (kva >= 0::numeric),
  engine_brand text NOT NULL,
  cpcb text NOT NULL,
  price numeric,
  price_on_request boolean NOT NULL DEFAULT false,
  moq integer NOT NULL DEFAULT 1,
  lead_time_days integer NOT NULL DEFAULT 21,
  stock USER-DEFINED NOT NULL DEFAULT 'in_stock'::product_stock_enum,
  short_desc text,
  full_desc text,
  tags ARRAY NOT NULL DEFAULT '{}'::text[],
  seo_title text,
  meta_desc text,
  inquiries integer NOT NULL DEFAULT 0,
  published_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  brand_id uuid,
  kwe numeric DEFAULT (kva * 0.8),
  view_count integer NOT NULL DEFAULT 0,
  is_featured boolean NOT NULL DEFAULT false,
  is_best_seller boolean NOT NULL DEFAULT false,
  created_by uuid,
  updated_by uuid,
  CONSTRAINT products_pkey PRIMARY KEY (id),
  CONSTRAINT products_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.product_categories(id),
  CONSTRAINT products_brand_id_fkey FOREIGN KEY (brand_id) REFERENCES public.product_brands(id),
  CONSTRAINT products_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id),
  CONSTRAINT products_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES auth.users(id)
);
CREATE TABLE public.profiles (
  user_id uuid NOT NULL,
  role USER-DEFINED NOT NULL DEFAULT 'Sales Executive'::app_role,
  full_name text,
  phone text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  avatar_url text,
  CONSTRAINT profiles_pkey PRIMARY KEY (user_id),
  CONSTRAINT profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.quote_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_id uuid NOT NULL,
  product_id uuid,
  quantity integer NOT NULL DEFAULT 1,
  unit_price numeric,
  line_total numeric,
  product_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quote_items_pkey PRIMARY KEY (id),
  CONSTRAINT quote_items_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT quote_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.quotes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_number text UNIQUE,
  lead_id uuid,
  created_by_user_id uuid,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::quote_status_enum,
  currency text NOT NULL DEFAULT 'INR'::text,
  total_amount numeric NOT NULL DEFAULT 0,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  sent_at timestamp with time zone,
  accepted_at timestamp with time zone,
  rejected_at timestamp with time zone,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quotes_pkey PRIMARY KEY (id),
  CONSTRAINT quotes_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT quotes_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.profiles(user_id)
);
CREATE TABLE public.security_settings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  two_factor boolean NOT NULL DEFAULT true,
  ip_whitelist boolean NOT NULL DEFAULT false,
  audit_log boolean NOT NULL DEFAULT true,
  session_timeout_minutes integer NOT NULL DEFAULT 480,
  login_attempts integer NOT NULL DEFAULT 5,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT security_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.seo_metadata (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  page_slug text NOT NULL UNIQUE,
  title text,
  description text,
  og_title text,
  og_description text,
  og_image_url text,
  canonical_url text,
  robots text DEFAULT 'index, follow'::text,
  structured_data jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT seo_metadata_pkey PRIMARY KEY (id)
);
CREATE TABLE public.site_settings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_name text,
  tagline text,
  website text,
  phone1 text,
  phone2 text,
  email text,
  admin_email text,
  whatsapp text,
  address text,
  linkedin text,
  facebook text,
  instagram text,
  youtube text,
  twitter text,
  gmaps_key text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT site_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.whatsapp_messages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  thread_id uuid NOT NULL,
  lead_id uuid,
  message_direction USER-DEFINED NOT NULL,
  sender USER-DEFINED NOT NULL,
  external_message_id text,
  external_contact_id text,
  message_text text,
  status text,
  received_at timestamp with time zone,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT whatsapp_messages_pkey PRIMARY KEY (id),
  CONSTRAINT whatsapp_messages_thread_id_fkey FOREIGN KEY (thread_id) REFERENCES public.lead_whatsapp_threads(id),
  CONSTRAINT whatsapp_messages_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id)
);